# Astro / Node build artifacts
node_modules/
dist/
.astro/

# Local env (the release version is injected at deploy time; see pages.yml)
.env

# install.sh is composed into public/ at build time from scripts/ (its SSOT) — never committed
public/install.sh
