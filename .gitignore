# build output
/bin/
/dist/

# exported recipes are derived share artifacts (source + adoption protocol), regenerated by
# `sporo export` — never source
.sporo/exports/

# go workspace files are never committed (the module builds without one — GOWORK=off is a gate)
go.work
go.work.sum

# local env / editor
.DS_Store
.env
.env.*
